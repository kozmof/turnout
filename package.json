{
  "scripts": {
    "generate": "PATH=\"$HOME/go/bin:$(go env GOPATH)/bin:$PATH:./node_modules/.bin\" buf generate",
    "test": "pnpm --dir packages/ts/runtime test && pnpm --dir packages/ts/scene-runner test && (cd packages/go/converter && go test ./...)"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.66.1",
    "@bufbuild/protoc-gen-es": "^2.11.0"
  }
}
